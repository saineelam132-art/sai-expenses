org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Keeps `:core:test` (a pure-JVM module) from needing the Android Gradle Plugin / Google Maven
# to be resolvable — only the subprojects a task actually touches get configured.
org.gradle.configureondemand=true
